# 📜 Scripts de la Aplicación

Este directorio contiene los scripts de ayuda para la aplicación.

## Scripts Disponibles

### `../run.sh`
Script principal para ejecutar la aplicación. Verifica dependencias y ejecuta la aplicación.

**Uso:**
```bash
./run.sh
```

### `../install_dependencies.sh`
Script para instalar todas las dependencias del sistema necesarias para PySide6 y FFmpeg.

**Uso:**
```bash
./install_dependencies.sh
```

## Notas

- Todos los scripts deben tener permisos de ejecución: `chmod +x script.sh`
- El script `install_dependencies.sh` requiere permisos de administrador (sudo)
